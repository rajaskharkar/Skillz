<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Main app theme -->
    <style name="Theme.Skillz" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Splash theme -->
    <style name="Theme.Skillz.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#3F8F8B</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.Skillz</item>
    </style>

</resources>